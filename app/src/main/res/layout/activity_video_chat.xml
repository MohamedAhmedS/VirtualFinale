<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activites.VideoChatActivity">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <FrameLayout
            android:id="@+id/subscriber_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#292929">

            <TextView
                android:id="@+id/tvTimer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="top|center"
                android:layout_marginTop="40dp"
                android:translationZ="10dp"
                android:background="#10000000"
                android:gravity="center"
                android:text="10:00"
                android:textColor="#ffffff"
                android:textSize="40sp" />

            <FrameLayout
                android:id="@+id/publisher_container"
                android:layout_width="90dp"
                android:layout_height="120dp"
                android:layout_gravity="bottom|end"
                android:layout_margin="10dp"
                android:background="@color/colorPrimaryDark"/>

        </FrameLayout>

    </FrameLayout>


    <ImageView
        android:id="@+id/close_video_chat_btn"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_alignParentBottom="true"
        android:layout_margin="20dp"
        android:background="@drawable/cancel_call_bg"
        android:padding="5dp"
        android:src="@drawable/cancel_call" />

    <TextView
        android:id="@+id/tvNoVideo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="Video Disabled"
        android:visibility="gone"
        android:textColor="@color/colorWhite" />

    <TextView
        android:id="@+id/tvNoAudio"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvNoVideo"
        android:layout_centerHorizontal="true"
        android:layout_gravity="center"
        android:text="Audio Disabled"
        android:visibility="gone"
        android:textColor="@color/colorWhite" />

</RelativeLayout>